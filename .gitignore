.next
node_modules

# Environment variables & secrets
.env
.env*.local
.env.local
.env.production

# Lock and process files
scripts/.youtube-sync.lock

# Migration artifacts — regenerate with: npm run migrate
data/media-raw-backup.ndjson